import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: set this once the domain is chosen (used for canonical URLs / sitemaps)
  // site: 'https://goodloe.me',
});
